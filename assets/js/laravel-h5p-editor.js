(function ($) {

    // setting for inside editor
    $.ajaxSetup({
        /*headers: {
            'X-CSRF-TOKEN': window.parent.Laravel.csrfToken
        },*/
        dataType: 'json',
    });

})(H5P.jQuery);
